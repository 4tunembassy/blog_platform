# Frontend Web (Public Site)

This folder will hold the Next.js app for the public website.

## Create (first time)
From `frontend/web`:

```bash
pnpm dlx create-next-app@latest . --ts --eslint --app --tailwind --src-dir --import-alias "@/*"
```

Then run:
```bash
pnpm dev
```
